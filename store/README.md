# AgileBot Store (HF Spaces)

Marketing + product-listing site for the autonomous business.

Deploy: create a Hugging Face Space (Gradio SDK), upload these files, set
Space secret `GUMROAD_TOKEN` from `secrets.env`. The site pulls live products
from the Gumroad API.

Local test: `pip install -r requirements.txt && python app.py`
